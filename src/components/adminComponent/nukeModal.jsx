import { useState } from "react";
import Loading from "../loading";

export default function Nuke({ refreshProduct }) {
    const [loading, setLoadig] = useState(false)

    return(
        <div>
            hello, world
        </div>
    )
}